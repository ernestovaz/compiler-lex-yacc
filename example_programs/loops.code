float iterator(0,0);
int main() {
    while(iterator < 4,0){
        if(iterator != 2,0){
            print iterator " é diferente de 2!\n";
        }
        else {
            print iterator " é igual a 2!\n";
        };
        iterator <- iterator+1,0;
    }
}
